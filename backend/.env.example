# =========================
# 🚀 SERVER CONFIGURATION
# =========================
PORT=5000  # Cổng mà server sẽ chạy (mặc định: 5000)

# =========================
# 📦 DATABASE CONFIGURATION (SQL Server)
# =========================
MSSQL_URL=localhost  # Địa chỉ SQL Server (VD: localhost, 192.168.1.100,...)
DB_NAME=MyDatabase   # Tên database

# Nếu sử dụng SQL Server Authentication:
DB_USER=sa           # Tên người dùng (VD: sa)
DB_PASSWORD=your_password  # Mật khẩu

# Nếu sử dụng Windows Authentication, hãy bỏ trống DB_USER và DB_PASSWORD
# (Windows Authentication sẽ được tự động kích hoạt)
DB_DRIVER=msnodesqlv8  # Driver kết nối (mặc định: msnodesqlv8)

# =========================
# ⚠️ HƯỚNG DẪN:
# 1️⃣ Đổi tên file này thành ".env" và cập nhật thông tin phù hợp với hệ thống của bạn.
# 2️⃣ KHÔNG commit file ".env" lên GitHub để bảo mật thông tin.
